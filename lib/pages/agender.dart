import 'package:flutter/material.dart';

class AgenderPage extends StatelessWidget {
  const AgenderPage({super.key});

  @override
  Widget build(BuildContext context){
    return Container(
      color:Colors.red,
    );
  }
}
